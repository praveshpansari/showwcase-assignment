import axios from "axios";

const randomUserAPI = "https://api.randomuser.me/";

export const fetchRandomUser = async (): Promise<any> => {
  // Implement fetching random user details from the third-party API
};
